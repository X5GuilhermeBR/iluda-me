import styled from "styled-components";

export const ButtonStyles = styled.a`
  padding: 10px;
  height: 100%;
  color: white;
  cursor: pointer;
  font-size: 14px;

  :hover {
    background-color: #eabc54; /* (150% zoom - Note: if the zoom is too large, it will go outside of the viewport) */
    color: #2a2827;
  }
`;
